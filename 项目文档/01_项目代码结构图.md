# Void 项目代码结构图
生成时间: 2026-04-02

## 项目概览

```
void-main/
├── .build/                 # 构建配置
├── .vscode/               # VSCode 配置
├── build/                 # 构建脚本和工具 (287 items)
├── cli/                   # 命令行接口 (73 items)
├── extensions/            # 内置扩展 (2085 items)
├── resources/             # 静态资源 (108 items)
├── scripts/               # 脚本文件 (34 items)
├── src/                   # 源代码 (5373 items)
│   ├── bootstrap-*.ts     # 启动引导文件
│   ├── main.ts            # 主入口
│   ├── vs/                # VSCode/Void 核心代码 (5209 items)
│   └── vscode-dts/        # VSCode 类型定义 (138 items)
├── test/                  # 测试文件 (111 items)
└── 项目文档/              # 本文档目录
```

## 核心架构

### 1. 进程架构

```
┌─────────────────────────────────────────────────────────┐
│                     主进程 (Main Process)                  │
│  src/vs/code/electron-main/                              │
│  - main.ts                                               │
│  - 窗口管理                                               │
│  - 系统资源                                               │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│                    渲染进程 (Renderer Process)             │
│  src/vs/workbench/                                       │
│  - browser/     - UI 界面                                │
│  - common/      - 共享代码                               │
│  - contrib/     - 功能模块                               │
│  - services/    - 核心服务                               │
└─────────────────────────────────────────────────────────┘
```

### 2. 核心模块结构

```
src/vs/
├── base/                  # 基础工具类 (464 items)
│   ├── common/           # 通用工具
│   ├── browser/          # 浏览器环境工具
│   └── node/             # Node.js 环境工具
│
├── platform/             # 平台抽象层 (635 items)
│   ├── configuration/    # 配置管理
│   ├── files/          # 文件系统
│   ├── keybinding/     # 快捷键
│   ├── storage/        # 存储
│   └── windows/        # 窗口管理
│
├── editor/               # 编辑器核心 (1129 items)
│   ├── browser/        # 编辑器 UI
│   ├── common/         # 编辑器逻辑
│   └── contrib/        # 编辑器扩展
│
├── workbench/            # 工作台 (2933 items)
│   ├── browser/        # 工作台 UI
│   ├── common/         # 工作台逻辑
│   ├── contrib/        # 功能模块
│   │   ├── void/       # Void 核心功能 (100 items)
│   │   ├── chat/       # 聊天功能 (275 items)
│   │   ├── notebook/   # 笔记本 (266 items)
│   │   ├── terminal/   # 终端 (113 items)
│   │   └── ...
│   └── services/       # 工作台服务
│
└── code/                 # 应用入口 (25 items)
    ├── browser/        # 浏览器入口
    ├── electron-main/  # 主进程入口
    └── node/           # Node.js 入口
```

### 3. Void 专属模块

```
src/vs/workbench/contrib/void/
├── browser/                          # 浏览器端代码
│   ├── react/                       # React UI 组件
│   │   └── src/
│   │       ├── void-onboarding/     # 引导页面
│   │       ├── void-panel/          # 侧边栏面板
│   │       ├── void-settings/       # 设置页面
│   │       └── util/services.tsx    # React 服务访问器
│   │
│   ├── chatThreadService.ts         # Agent 循环 + 对话线程管理
│   ├── editCodeService.ts           # 代码编辑服务（Apply/Diff/模糊匹配）
│   ├── toolsService.ts              # 内置工具执行引擎（含 batch_edit）
│   ├── terminalToolService.ts       # 终端工具集成
│   ├── convertToLLMMessageService.ts # LLM 消息构造 + 活跃上下文注入
│   ├── contextCompactionService.ts  # 【Phase 3】上下文压缩服务
│   ├── contextGatheringService.ts   # 光标附近代码收集
│   ├── autocompleteService.ts       # FIM 自动补全
│   ├── sidebarPane.ts               # 侧边栏面板 UI
│   ├── sidebarActions.ts            # 侧边栏快捷键（Ctrl+L）
│   ├── quickEditActions.ts          # 快速编辑（Ctrl+K）
│   ├── voidSCMService.ts            # SCM 集成（AI Commit Message）
│   ├── hookService.ts               # 【Phase 2】工具钩子系统
│   ├── turnCheckpointService.ts     # 【Phase 2】Turn 级检查点服务
│   └── void.contribution.ts         # Void 模块注册中心
│
├── common/                           # 共享类型和服务
│   ├── chatThreadServiceTypes.ts    # 聊天消息类型定义
│   ├── toolsServiceTypes.ts         # 工具参数/结果类型（含 batch_edit）
│   ├── contextCompactionTypes.ts    # 【Phase 3】上下文压缩类型
│   ├── codeIndexTypes.ts            # 【Phase 1】代码索引类型
│   ├── hookTypes.ts                 # 【Phase 2】钩子系统类型
│   ├── turnCheckpointTypes.ts       # 【Phase 2】检查点类型
│   ├── sendLLMMessageService.ts     # LLM 消息发送服务
│   ├── sendLLMMessageTypes.ts       # LLM 消息类型
│   ├── voidSettingsService.ts       # Void 设置服务
│   ├── voidSettingsTypes.ts         # Void 设置类型
│   ├── voidModelService.ts          # 模型管理服务
│   ├── mcpService.ts                # MCP 外部工具服务
│   ├── directoryStrService.ts       # 目录结构字符串服务
│   ├── modelCapabilities.ts         # 模型能力检测
│   └── prompt/
│       └── prompts.ts               # 系统提示 + 工具定义（含 batch_edit）
│
├── electron-sandbox/                 # Electron 沙箱代码
└── electron-main/
    └── sendLLMMessage.ts            # 主进程 LLM 请求发送
```

## 功能模块分布

### 核心功能模块 (workbench/contrib/)

| 模块 | 文件数 | 主要功能 |
|------|--------|---------|
| chat | 275 | AI 聊天、对话管理 |
| notebook | 266 | Jupyter 笔记本 |
| void | 100 | Void 核心 AI 功能 |
| terminal | 113 | 终端模拟器 |
| terminalContrib | 102 | 终端扩展 |
| testing | 74 | 测试框架 |
| extensions | 62 | 扩展管理 |
| search | 57 | 全局搜索 |
| debug | 107 | 调试器 |
| scm | 24 | 源代码管理 |

### 欢迎页面相关模块

```
src/vs/workbench/contrib/welcomeGettingStarted/ (36 items)
├── browser/
│   ├── gettingStarted.ts          # 欢迎页面主逻辑
│   ├── gettingStartedInput.ts     # 输入处理
│   ├── gettingStarted.contribution.ts  # 模块注册
│   └── media/
│       └── gettingStarted.css     # 样式
├── common/
│   └── gettingStartedContent.ts   # 内容定义
└── ...
```

## 文件命名规范

- `*.ts` - TypeScript 源码
- `*.browser.ts` - 浏览器端代码
- `*.common.ts` - 跨平台代码
- `*.node.ts` - Node.js 端代码
- `*.electron-main.ts` - Electron 主进程代码
- `*.contribution.ts` - 模块注册
- `*.service.ts` - 服务类
- `*.test.ts` - 测试文件

## 核心服务列表

```
src/vs/workbench/services/
├── editor/           # 编辑器服务
├── files/           # 文件服务
├── configuration/   # 配置服务
├── keybinding/      # 快捷键服务
├── storage/         # 存储服务
├── telemetry/       # 遥测服务
├── themes/         # 主题服务
├── extensions/     # 扩展服务
├── search/         # 搜索服务
├── debug/          # 调试服务
└── ...
```

---
生成时间: 2026-04-02
执行工具: Cascade AI Assistant
